package eu.kanade.tachiyomi.source.model

/** Stub mirror of the host UpdateStrategy enum (referenced by SManga.update_strategy). */
enum class UpdateStrategy {
    ALWAYS_UPDATE,
    ONLY_FETCH_ONCE,
}
