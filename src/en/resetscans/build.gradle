ext {
    extName = 'Reset Scans'
    extClass = '.ResetScans'
    extVersionCode = 10
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation project(':lib-multisrc:madara')
}
