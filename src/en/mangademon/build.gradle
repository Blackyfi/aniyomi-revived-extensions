ext {
    extName = 'Manga Demon'
    extClass = '.MangaDemon'
    extVersionCode = 18
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    // keiyoushi.utils.tryParse — bundled (host does not provide it).
    implementation project(':core')
}
